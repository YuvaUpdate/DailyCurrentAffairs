const fs=require('fs');const path=require('path');const start=path.join('android');let files=[];function walk(d){if(!fs.existsSync(d))return;for(const n of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,n.name); if(n.isDirectory())walk(p); else if(p.toLowerCase().endsWith('.png'))files.push(p);} }walk(start);const out=[];for(const f of files){try{const b=fs.readFileSync(f); if(b.length>=3&&b[0]==0xFF&&b[1]==0xD8&&b[2]==0xFF)out.push(f);}catch(e){} }fs.writeFileSync('png_jpeg_list.txt', out.join('\n'));console.log('FOUND',out.length);
