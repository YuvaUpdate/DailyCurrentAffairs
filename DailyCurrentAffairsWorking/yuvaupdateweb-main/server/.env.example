# Cloudflare R2 credentials and configuration
# IMPORTANT: Do NOT commit real secrets. Fill these in your local `server/.env` or in your host's secret manager.
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_ACCOUNT_ID=your_account_id
R2_BUCKET=your_bucket_name
# Optional: custom endpoint or public URL. If omitted, default patterns are used.
R2_ENDPOINT=
R2_PUBLIC_URL=
R2_REGION=auto

# NOTE: If the values above were real keys you pasted by mistake, rotate them immediately in Cloudflare and
# replace them here with placeholders. Do not commit real secrets to Git.
